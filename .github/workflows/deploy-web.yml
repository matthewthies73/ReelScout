name: Deploy Web

on:
  push:
    branches: [main]
    paths: ['composeApp/**', 'shared/**']

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - run: chmod +x gradlew
      - run: ./gradlew :composeApp:wasmJsBrowserDistribution
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy composeApp/build/dist/wasmJs/productionExecutable --project-name=reelscout-web
      # Smoke test: the custom domain serves the app, not a Pages error page.
      - name: Check the live site
        run: curl -fsS --retry 5 --retry-delay 5 --retry-all-errors https://reelscout.bitterinfantproductions.com/ | grep -q '<title>ReelScout</title>'
